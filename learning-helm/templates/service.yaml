apiVersion: v1
kind: Service
metadata:
  name: {{ include "learning-helm.fullname" . }}
  labels:
    {{- include "learning-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "learning-helm.selectorLabels" . | nindent 4 }}
